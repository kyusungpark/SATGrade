import React from 'react';
import { Text } from 'react-native';

import { Center } from '../../components';

const CheckAKScreen = () => {
	return (
		<Center>
			<Text>SAT Screen</Text>
		</Center>
	);
};

export default CheckAKScreen;
